# m1_hello_mesh

Not started. Goal: cube/static mesh loaded from glTF, orbiting camera, Low-Poly Retro
unlit pipeline active (docs/02 section 4, docs/03 section 6).

Needs (see engine/include/engine/{rhi,renderer,core}/README.md):
`RHIBuffer`, `RHIPipeline`, `MeshLoader` (cgltf), `ForwardLitPipeline` (unlit), `Camera`.
Test asset: `assets/m1_cube.glb`.
