# m2_hello_scene

Not started. Goal: a handful of objects in the scene, frustum culling active, first real
use of the Job System (docs/02 section 4, docs/03 section 7).

Needs: `ecs/Entity`, `ecs/World`, `TransformComponent`/`MeshComponent`, `jobs/JobSystem`,
`renderer/FrustumCuller`, `core/Application` (new orchestrator — every milestone from here
adds a phase to its loop).
