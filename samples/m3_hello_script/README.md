# m3_hello_script

Not started. Goal: an object moves via keyboard input, minimal Script System
(docs/02 section 4, docs/03 section 8).

Needs: `script/ScriptComponent`, `script/ComponentHandle`, `script/ScriptRegistry`
(`REGISTER_SCRIPT`), `script/Expose` (`EXPOSE`), `platform/InputState` + `InputSystem`
(keyboard only). First script: `scripts/MoveScript.h`.
